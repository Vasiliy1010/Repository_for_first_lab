spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: missions-archive
  datasource:
    url: jdbc:postgresql://localhost:5432/magic_archive
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB